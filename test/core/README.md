# core tests

Pure Dart tests for `lib/core/`. No `testWidgets`, no pumping, no devices —
these run identically on macOS, Windows and the Linux CI runner.

When fixing a bug, add the failing test here first, then fix it.
